[project]
name = "claude-bash-approve"
version = "0.1.0"
description = "Shared installer and hook runtime for Claude Code, OpenCode, Codex, and pi"
requires-python = ">=3.11"

[tool.uv]
package = false
